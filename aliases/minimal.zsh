# Minimal mode aliases
# Clean, minimal set of aliases

# Basic navigation
alias ll="ls -la"
alias la="ls -A"
alias l="ls -CF"
alias ..="cd .."
alias ...="cd ../.."

# Basic file operations
alias cp="cp -i"
alias mv="mv -i"
alias rm="rm -i"

# Just shortcuts
alias jb="just build"
alias js="just source"
alias jall="just all"

echo "⚡ Minimal mode loaded" 